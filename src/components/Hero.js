import React from 'react';
import { t } from 'ttag';

const Hero = () => (
  <div className="hero">
    <h1>{t`Nengo`}</h1>
    <h3>Converts Gregorian years into traditional Japanese years</h3>
  </div>
);

export default Hero;
